.last(class="lg:py-[200px]")
  .container
    .content.text-center.relative(class="py-[60px]")
      .text
        h1.text-3xl.font-semibold We are trusted by over 5000+ clients.
        h1.text-3xl.font-semibold Join them now and grow your busniess.
      .btns(class="pt-[80px]")
        button.blue-btn.opacity-0.mr-3(
          data-parralex="true",
          animates="animate-up",
          class="translate-y-[60px]"
        ) Get Started
        button.green-btn.opacity-0(
          data-parralex="true",
          animates="animate-up",
          class="translate-y-[40px]"
        ) Free Trial
      .dots2.absolute.hidden(class="lg:block")
      .dots2.absolute.right-0.top-0.bg-red-200.hidden(class="rounded-[50%] lg:block")
      .dots2.absolute.right-0.hidden(class="lg:block")
      .dots2.absolute.top-0.rotate-45.hidden(class="lg:block")
      .dots2.absolute.top-10.rotate-90.hidden
